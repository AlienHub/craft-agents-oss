# v0.1.8 - Responses API Hotfix

## Features

None.

## Improvements

- **Apple Silicon release only** The GitHub release pipeline now publishes macOS arm64 artifacts only.

## Bug Fixes

- **OpenAI Responses history repair** Fixed upgraded sessions failing with `input[N].id` length validation errors by removing overlong optional item ids before sending requests, while preserving tool `call_id` links.

## Breaking Changes

None.
